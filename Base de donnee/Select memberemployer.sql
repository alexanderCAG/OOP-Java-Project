use `Company`;

-- Affichage job + Mise a jour
-- select distinct namejob from job;

-- Affichage des nouveaux emplois (nom et prenom)
-- select namemem from memberemployer;


-- Ajouter des nouveaux jobs
-- INSERT INTO `Company`.Agency ( nameag ) VALUES ('Agence DAG');

-- Afficher les candidats(Quelles informations retourner?)
-- select * from jobseeker;

-- Supprimer des jobs
-- delete from job where namejob='';

-- Evaluer la popularite des emplois (sur le programme il faudra additionner les deux)
-- select count(namemem) from memberemployer where job=' ';
-- select count(namenew) from newemployer where job='';

-- Verifier si un compte existe deja
-- select *from memberemployer;
-- select *from newemployer;
-- select *from Jobseeker;

-- employer 